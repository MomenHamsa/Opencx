/** @type {import('next').NextConfig} */
const nextConfig = {
  // Traces and baselines are plain JSON files on disk (see src/lib/store.ts).
  // Nothing here needs a database, so there is nothing to configure yet.
};

export default nextConfig;
