node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# Secrets. Only .env.example is committed.
.env
.env.local

# Runtime artifacts: traces and eval runs are regenerated by clicking "Run Evaluation".
data/traces/
data/runs/

# Authored content: your knowledge base, test cases and prompts. Deliberately not
# committed — this is real work content, and the bundled samples in src/lib/seed/
# reseed it automatically on a fresh checkout.
data/config/

.DS_Store
